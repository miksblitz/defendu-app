{
  "version": 2,
  "buildCommand": "echo 'No build needed for API routes'",
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/",
      "destination": "/api"
    },
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
