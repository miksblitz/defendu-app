# Copy to .env and adjust. Expo reads EXPO_PUBLIC_* at bundle time — restart dev server after changes.

# Vercel deployment root URL (no trailing slash). Defaults to https://defendu-app.vercel.app if unset.
EXPO_PUBLIC_API_BASE_URL=https://defendu-app.vercel.app
